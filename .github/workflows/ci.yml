name: Simple CI

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: zricethezav/gitleaks:v8.18.3
    steps:
      - uses: actions/checkout@v3
      - run: gitleaks version && gitleaks detect -v --no-banner --source=. --redact=50 --log-opts=HEAD
